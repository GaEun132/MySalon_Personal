global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # Mac/Windows의 경우 host.docker.internal을 사용해 로컬 스프링 앱에 접근
      - targets: ['spring:8080']